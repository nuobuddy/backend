# Server
PORT=3000
NODE_ENV=development

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=nuobuddy
DB_PASSWORD=
DB_DATABASE=nuobuddy

# TypeORM options
# Set to true only in development — never in production
DB_SYNCHRONIZE=false
DB_LOGGING=false
DB_SSL=false

# JWT
JWT_SECRET=
JWT_EXPIRES_IN=7d
